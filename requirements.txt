# Core
torch>=2.2.0
transformers>=4.44.0
datasets>=2.20.0
accelerate>=0.33.0
peft>=0.12.0
trl>=0.9.6

# Quantization backends
bitsandbytes>=0.43.1
autoawq>=0.2.5        # requires CUDA; skip on CPU-only environments
auto-gptq>=0.7.1       # requires CUDA; skip on CPU-only environments

# Serving
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
requests>=2.32.0

# Benchmarking & analysis
psutil>=6.0.0
matplotlib>=3.9.0
pandas>=2.2.0

# Data / evaluation utilities
sqlglot>=25.0.0
pyyaml>=6.0.1

# Testing
pytest>=8.2.0
